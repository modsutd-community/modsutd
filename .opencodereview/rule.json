{
  "_comment": [
    "Project layer (priority 2 of 4) for open-code-review, read by",
    ".github/workflows/code-review.yml. The action checks out the TRUSTED",
    "BASE, so a pull request cannot widen its own excludes, and an edit here",
    "does nothing to any review until it is merged to main.",
    "",
    "`exclude` filters PER FILE, which the workflow's paths-ignore cannot:",
    "paths-ignore skips a run only when EVERY changed file matches, so one",
    "edited .txt beside 300 course records would send all 301 to the model.",
    "That is what answered HTTP 413 and then retried into 429s. Both layers",
    "stay: paths-ignore means a data-only pull request starts no runner at",
    "all, and this means a MIXED one still reviews only the code.",
    "",
    "What is excluded is generated or record data, which is not what an LLM",
    "reviewer is good for: a course record is checked against the source URL",
    "cited in the pull request body, and sync-data.mjs fails the build on a",
    "bad mapName or an unknown venue.",
    "",
    "ONE RULE MATCHES PER FILE. `ocr rules check <path>` prints a single",
    "`Pattern:`, so these do not stack: the first entry whose glob matches is",
    "the whole instruction that file's review gets. Two consequences, both of",
    "which were live here until a review found them.",
    "",
    "  1. NO CATCH-ALL. A `**/*` entry matched every file and sat at index 0,",
    "     so all seven specific rules below it were dead. That is why the",
    "     reporting discipline is repeated in each entry rather than factored",
    "     out. It reads as a violation of the say-a-thing-once rule in",
    "     CLAUDE.md and it is not: the alternative was five rules that never",
    "     ran.",
    "  2. NO DUPLICATE PATHS, and most specific FIRST. Two entries shared",
    "     `tools/scraper/**/*.py` and two shared `frontend/src/**`, so the",
    "     second of each pair could never match. `frontend/src/workbench/**`",
    "     precedes `frontend/src/**` for the same reason, and carries the",
    "     TypeScript rules itself because it shadows them.",
    "",
    "WHY THESE READ AS A LIST OF THINGS NOT TO SAY. The reviewer sees the diff",
    "hunk and nothing else: not CLAUDE.md, not the rest of the file, not the",
    "commit message. So it cannot tell a bug from a decision, and its wrong",
    "findings are all one shape - proposing to revert something deliberate, in",
    "the same red badge as a real defect. A rule naming the decision is the",
    "only channel there is for telling it. Add one whenever a review argues",
    "with a decision the repo has already made, and keep the reason in the",
    "rule so a reader knows why it is there.",
    "",
    "tools/ci/ is EXCLUDED because it is vendored: third-party files kept at a
    named upstream commit and never edited here. A finding in one of them is
    addressed by refreshing the file or by dropping the dependency, never by
    patching it in place, so reviewing them produces work nobody can do.

    Check any pattern with `ocr rules check <path>`, and check a whole diff",
    "with `ocr review --preview --from main --to <branch>`, which prints the",
    "verdict per file (user_exclude, unsupported_ext, ...) and calls no LLM."
  ],
  "exclude": [
    "data/**",
    "frontend/public/data/**",
    "**/package-lock.json",
    "**/*.geojson",
    "tools/ci/**"
  ],
  "rules": [
    {
      "path": "frontend/src/workbench/**",
      "rule": "HOW TO REPORT, which governs the FORM of a finding and never whether a defect is worth reporting: nothing here is a reason to stay quiet about a real bug. Say nothing you cannot point at inside the diff. No finding that asks the author to go and verify something, and none opening 'if X is ever' or 'in some environments'. One finding per root cause, not one per line it touches. High means lost user data, a leaked credential, or a gate that stopped gating; medium means a wrong result a user would see; everything else is low, including style, naming and accessibility polish. EXAMPLE. Good: \"planFile.ts:130 falls back to Object.values(migrated)[0] when the file has no plan for the tab you are on, so importing an AY2025-only backup into the AY2026 tab silently installs the AY2025 plan.\" That points at a line, states the input and names the consequence. Bad: \"If ExtLink is ever nested in a label, clicking it would both navigate and toggle. Please verify.\" That asks the author to read a file you did not read, and the answer was two lines above the hunk.\n\nTypeScript strict, no `any` (use `unknown`). Comments say WHY or HOW, never restate WHAT. Flag anything adding analytics, tracking, cookies, telemetry or a runtime dependency: all four are architecture invariants in CLAUDE.md. Flag a callback prop reaching an effect's dependency array where the effect builds an iframe or a subscription: an inline arrow is a new function every render and rebuilds it in a loop.\n\nTHE SITE IS LIVE AND HAS NO BACKEND, so every plan, layout, consent flag and parsed timetable sits in a browser or a gist no deploy reaches. Flag a renamed or removed localStorage key, gist section, JSON field or export shape where nothing reads the old name: that silently drops a student's data. A one-way migration that reads the old name once and rewrites the new one is the correct shape, not a finding.\n\nDELIBERATE HERE, DO NOT FLAG. ExtLink is not label-safe and its own doc comment says so; stopPropagation cannot make it so, because a label's activation is the browser's default rather than a React bubble, and preventDefault is what navigates. `.badgeWrap` is inline-flex because it IS the unit that wraps. `track.url ?? track.source` reads two scrapes with two spellings, and no record carries both. giscus boards are mounted once and hidden rather than unmounted, and rebuilt on a slow loop; CLAUDE.md says why each of those is load-bearing."
    },
    {
      "path": "frontend/e2e/**",
      "rule": "HOW TO REPORT, which governs the FORM of a finding and never whether a defect is worth reporting: nothing here is a reason to stay quiet about a real bug. Say nothing you cannot point at inside the diff. No finding that asks the author to go and verify something, and none opening 'if X is ever' or 'in some environments'. One finding per root cause, not one per line it touches. High means lost user data, a leaked credential, or a gate that stopped gating; medium means a wrong result a user would see; everything else is low, including style, naming and accessibility polish. EXAMPLE. Good: \"planFile.ts:130 falls back to Object.values(migrated)[0] when the file has no plan for the tab you are on, so importing an AY2025-only backup into the AY2026 tab silently installs the AY2025 plan.\" That points at a line, states the input and names the consequence. Bad: \"If ExtLink is ever nested in a label, clicking it would both navigate and toggle. Please verify.\" That asks the author to read a file you did not read, and the answer was two lines above the hunk.\n\nFind elements by a `data-act` hook or an accessible name, never by copy that gets reworded. Flag a test that cannot fail: a page.goto that remounts the component under test, a CSS assertion at a viewport where the cap never binds, a check below its own threshold, an assertion on a value the test itself set. Flag behaviour the diff changes that no test covers, when getting it wrong would show a student a wrong answer."
    },
    {
      "path": "frontend/src/**/*.{ts,tsx}",
      "rule": "HOW TO REPORT, which governs the FORM of a finding and never whether a defect is worth reporting: nothing here is a reason to stay quiet about a real bug. Say nothing you cannot point at inside the diff. No finding that asks the author to go and verify something, and none opening 'if X is ever' or 'in some environments'. One finding per root cause, not one per line it touches. High means lost user data, a leaked credential, or a gate that stopped gating; medium means a wrong result a user would see; everything else is low, including style, naming and accessibility polish. EXAMPLE. Good: \"planFile.ts:130 falls back to Object.values(migrated)[0] when the file has no plan for the tab you are on, so importing an AY2025-only backup into the AY2026 tab silently installs the AY2025 plan.\" That points at a line, states the input and names the consequence. Bad: \"If ExtLink is ever nested in a label, clicking it would both navigate and toggle. Please verify.\" That asks the author to read a file you did not read, and the answer was two lines above the hunk.\n\nTypeScript strict, no `any` (use `unknown`). Comments say WHY or HOW, never restate WHAT. Flag anything adding analytics, tracking, cookies, telemetry or a runtime dependency: all four are architecture invariants in CLAUDE.md. Flag a callback prop reaching an effect's dependency array where the effect builds an iframe or a subscription: an inline arrow is a new function every render and rebuilds it in a loop.\n\nTHE SITE IS LIVE AND HAS NO BACKEND, so every plan, layout, consent flag and parsed timetable sits in a browser or a gist no deploy reaches. Flag a renamed or removed localStorage key, gist section, JSON field or export shape where nothing reads the old name: that silently drops a student's data. A one-way migration that reads the old name once and rewrites the new one is the correct shape, not a finding.\n\nDELIBERATE HERE, DO NOT FLAG.\n1. The .ics UID is keyed on mod + type + date and NOTHING else. Room, time and title are left out because they are what gets corrected, and including them turned a fix into a duplicate term. So a changed SUMMARY updates an event in place: do not report a title change as breaking existing exports.\n2. TimetableEvent carries fields that are LOCAL - venueName, section - meaning they never leave the browser, not that they are unused outside React. The .ics is generated client-side and downloaded. What leaves is exactly what eventsToSlots builds, and a test asserts that key set, because widening it is a Discussion under CLAUDE.md invariant 5.\n3. A capstone's CC section is dropped on purpose: it is a project team rather than a cohort, and the parser resolves each row against the nearest PRECEDING group header, so nothing inherits a section across groups. A case in timetableParser.test.ts pins it.\n4. A schedule entry is one weekday and one time, with no notion of which weeks it meets. A fortnightly class is stored as weekly. That is a known limit written down in the enrolment skill, not a defect to report."
    },
    {
      "path": "tools/scraper/**/*.py",
      "rule": "HOW TO REPORT, which governs the FORM of a finding and never whether a defect is worth reporting: nothing here is a reason to stay quiet about a real bug. Say nothing you cannot point at inside the diff. No finding that asks the author to go and verify something, and none opening 'if X is ever' or 'in some environments'. One finding per root cause, not one per line it touches. High means wrong data reaching /data, a leaked credential, or a gate that stopped gating; medium means a wrong result a user would see; everything else is low, including style, naming and portability. EXAMPLE. Good: \"planFile.ts:130 falls back to Object.values(migrated)[0] when the file has no plan for the tab you are on, so importing an AY2025-only backup into the AY2026 tab silently installs the AY2025 plan.\" That points at a line, states the input and names the consequence. Bad: \"If ExtLink is ever nested in a label, clicking it would both navigate and toggle. Please verify.\" That asks the author to read a file you did not read, and the answer was two lines above the hunk.\n\nThis code reads SUTD's HTML, which changes without notice. Flag any parser that would write a THIN or EMPTY result over good data rather than refusing: a source answering 200 with almost nothing must never wipe a file, and a generator with no floor at all is the same defect waiting to happen. Flag file reads and writes that omit encoding=\"utf-8\": the repo's JSON is UTF-8 and the default on Windows is cp1252, which crashes before the scraper fetches anything. Flag anything a model proposes that could reach /data without being checked against the page text it quotes.\n\nDELIBERATE HERE, DO NOT FLAG. audit_prereqs.py and gather_minors.py report and never write, because a page can name a code and then disown it. gather_minors.py does not count 'the repo requires a code the page does not name' as drift: most minor pages say 'any HASS elective' and data/minors.json expands that into a real list, saying so in its own note, so counting it marks nearly every minor as drifting. sources/_http.py skips the cache write when a body is empty, because the read path treats a zero-byte file as a miss. mods_refresh.py exits 0 when some steps failed: steps are isolated so one redesigned page cannot hold the others hostage, and the run says which failed. canaries.py probes every scene id in data/venues against the tour's real tile URL and exits 0 whatever it finds: a host down for a minute must not take the catalogue refresh down with it, and the finding reaches the scrape pull request either way. A stale scene id being reported is the POINT - the report names the scene, and either the tour moved or the record is wrong, both of which a person needs to see. It sits in wave 1 because it writes nothing and so nothing orders it, and because mods walks 389 pages in that same wave: 26 requests taking five seconds cannot decide when the wave ends. OFF_SPACE_ADMIT in gather_mods.py is a person's decision to import a code SUTD lists outside its own undergraduate space, and its third field is the sentence on the page that justified it, re-read on every run. A course already in /data is reported and kept when the page loses that sentence, because a copy-edit must not silently drop a course from the catalogue; a code admitted there and never written is refused. elective_duplicate() asks whether the repo already owns the course a '<name> (Elective)' title re-lists, so a base course the repo does not hold is not a duplicate and writing it is the point - do not propose widening that lookup past data/courses, because the only listing that could answer more is the one being scraped. The pillar and term in that table are FALLBACKS passed as term_from_tags(default=...), not overrides: they exist because the page publishes no Term tag, and a tag it starts publishing is a better answer than a number typed into the table a year earlier. Do not propose making the pin win over the page. An admitted code does NOT go into undergrad_urls' off_space bucket: that bucket is what the run dropped, and the run prints the admitted ones on their own summary line for exactly the visibility a finding would ask for. refused_dup is the same shape - a title the (Elective) guard turns away is printed with both codes and counted in the summary, so a real course caught by it is visible in the run rather than silently missing."
    },
    {
      "path": ".github/workflows/*.yml",
      "rule": "HOW TO REPORT, which governs the FORM of a finding and never whether a defect is worth reporting: nothing here is a reason to stay quiet about a real bug. Say nothing you cannot point at inside the diff. No finding that asks the author to go and verify something, and none opening 'if X is ever' or 'in some environments'. One finding per root cause, not one per line it touches. High means wrong data reaching /data, a leaked credential, or a gate that stopped gating; medium means a wrong result a user would see; everything else is low, including style, naming and portability. EXAMPLE. Good: \"planFile.ts:130 falls back to Object.values(migrated)[0] when the file has no plan for the tab you are on, so importing an AY2025-only backup into the AY2026 tab silently installs the AY2025 plan.\" That points at a line, states the input and names the consequence. Bad: \"If ExtLink is ever nested in a label, clicking it would both navigate and toggle. Please verify.\" That asks the author to read a file you did not read, and the answer was two lines above the hunk.\n\nFlag `continue-on-error` at JOB level where a later job gates on `needs.<job>.result`: it rewrites the result to 'success' on failure and silently disables the fallback. Step level plus a job output reading `steps.<id>.outcome` is the working form. Flag a step whose exit code does not reflect what it actually did, and a fallback chain where a green check can be reached without the work happening. Flag any push to main that does not authenticate as a token with the admin role, because main's ruleset requires a reviewed pull request and only that role bypasses it. Flag a secret that is only checked after expensive work has already run. Flag a pull_request_target workflow that executes anything from the head ref, and permissions wider than the job needs."
    },
    {
      "path": "tools/telegram/**/*.py",
      "rule": "HOW TO REPORT. Say nothing you cannot point at inside the diff. No finding that asks the author to go and verify something, and none opening 'if X is ever' or 'in some environments'. One finding per root cause. A claim about what a registry entry can contain has to be checkable against data/telegram-groups.json, which is in this repo.\n\nWHAT THESE FILES DO. create_group.py makes one basic group per mod and exports its invite link. grant_admin.py is a daily sweep with three queues: `todo` hands a basic group to its first human joiner (migrate, then promote), `recheck` asks every live channel whether it still has a human admin and promotes the next joiner when it does not, and `done` leaves a chat whose term is over. participants.py holds the one pure reading the sweep makes of a participant list, asked of the members to pick who to promote and of the admins to see who already holds the chat. linkcrypt.py encrypts the link because the registry is committed to a public repo.\n\nFLAG. A credential reaching a log, a file or a commit. A call that could add a member directly rather than through an invite link, which earns PEER_FLOOD. An exception swallowed so that one bad chat silently stops the sweep for every other. A write to data/telegram-groups.json that drops a field another run needs to find the chat again: chatId, accessHash, linkEnc, expires.\n\nDELIBERATE HERE, DO NOT FLAG.\n1. The sweep runs daily and re-reads every live handed-over chat every time. An admin can leave on any day, so 'already handed over' is not a state that stays true, and there is no flag that could know it changed: only asking Telegram can. Do not propose a needs-recheck flag, do not propose gating on adminUserId, and do not report the daily run as wasted work.\n2. `recheck` is gated on supergroup AND accessHash AND an unexpired term, and deliberately NOT on adminGranted: the flag records what a run did, this queue is about what the chat IS, and a chat that promoted and then failed to pin is a supergroup with no flag that no other queue can reach. channels.getParticipants answers for a channel and not for a basic group, so an entry without accessHash has nothing this path could ask; and a term that is over is the `done` queue, where the chat belongs to its members and this account is walking out. Promoting somebody into a group modSUTD is leaving is the wrong move, not a missing case.\n3. The account running the sweep is excluded by id from both readings. Migration makes it the channel creator, a creator cannot be demoted, and without the exclusion the admin reading answers yes for every chat forever.\n4. One reader, sort_joiners, serves both lists on purpose. The caller passes the ADMIN list when the question is who holds the chat, so everyone in it holds a rank and only the filtering matters; sharing it is what stops a bot satisfying one reading and not the other.\n5. grant_admin.py does NOT leave at handover. Telegram revokes the invite links of a user who leaves, so the stored link dies with its membership. It leaves once the term is over, in the `done` queue.\n6. A re-promotion does not re-export the link. Nothing migrates there, and the link belongs to the account still sitting in the chat.\n7. adminGranted is written the moment the promotion lands, above the note and the pin, because the handover IS the promotion. Do not move it back below them.\n8. --self-check covers the pure reader and nothing else. Reaching Telegram needs TG_SESSION, which is full access to the throwaway account and belongs in two workflows' secrets; a CI job holding it would be a third place that token lives, on every pull request including one from a fork. Telegram upgrades a basic group to a supergroup on its own, and the old id survives as a tombstone whose getFullChat answers ChatParticipantsForbidden with no member list; moved_to() follows migrated_to and hands over on the channel instead. hand_over() is the single place that promotes, records and then pins, and its registry writes sit ABOVE the note deliberately. 9. The handover queue BREAKS on UserRestrictedError rather than continuing to the next chat. That error is about the account, so every later promotion in the run gets it too, and reaching it costs an irreversible migration each time. Do not report the break as one bad chat stalling the sweep: the two queues below it still run, and every other exception is still caught per chat."
    },
    {
      "path": "api/**/*.js",
      "rule": "HOW TO REPORT. Say nothing you cannot point at inside the diff. Never ask the author to verify that a changed constant was intended: a number moving in a diff is a decision somebody made, and 'please confirm this is intended' is not a finding. Report it only if you can name what it breaks.\n\nWHAT THESE ARE. Six stateless relays, server-side, reading process.env. They exist because a browser cannot hold a token, and they are NOT the backend that invariant 1 in CLAUDE.md forbids: the line is state, and none of them remembers a user between requests. They live at the repo ROOT rather than under frontend/ because Vercel's Root Directory cannot reach above itself and the build needs /data.\n\nFLAG. A secret reaching a response body, a log or a redirect. A parameter forwarded into a URL or a fetch without being validated first: telegram-groups.js was an open redirect until its code shape was checked. A VITE_-prefixed env var, which Vite inlines into the public bundle. A relay that starts remembering something between requests.\n\nDELIBERATE HERE, DO NOT FLAG.\n1. telegram-link.js's quota counter is an in-memory Map, per warm instance. A shared store is the backend this project does not have. A determined scraper gets more by forcing cold starts; a casual one does not, and the comment says so.\n2. The account-age threshold is a RANGE drawn per account as an HMAC, and the range starts at 0 so some accounts face no wait. A fixed number would tell a collector exactly how long to age a throwaway; the ceiling is what they have to plan around, and they cannot know which accounts drew it. Do not report the floor as a weakened gate.\n3. The refusal messages deliberately omit the threshold. Telling someone their own draw hands them the exact wait.\n4. Creating a batch chat is ungated on purpose. Whoever presses first gets the group made for the whole cohort, even if they cannot be shown the link themselves; the gate is on revealing a link, never on a cohort having a chat.\n5. These files are not linted. ESLint refuses a file above its own config's directory, and moving the config to the repo root to reach six handlers would drag every path in it up too. They are syntax-checked by node and covered by telegram-link.test.js."
    }
  ]
}
